steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - build
  - '-f'
  - 'docker/Dockerfile'
  - '-t'
  - 'gcr.io/$PROJECT_ID/cifar100-inference:cpu-latest'
  - '.'
images:
- 'gcr.io/$PROJECT_ID/cifar100-inference:cpu-latest'
timeout: '1200s'
